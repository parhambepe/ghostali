<div align="center" dir="rtl">

# 👻 روح‌گرام پرو (GhostGram PRO)
### *دستیار، رفیق و یوزربات مخفی تلگرام قدرت‌گرفته از هوش مصنوعی*

<p align="center">
  <img src="https://img.shields.io/badge/Python-3.10%20%7C%203.11%20%7C%203.12-3776AB?style=for-the-badge&logo=python&logoColor=white" alt="Python" />
  <img src="https://img.shields.io/badge/Telethon-MTProto%20v2-2CA5E0?style=for-the-badge&logo=telegram&logoColor=white" alt="Telethon" />
  <img src="https://img.shields.io/badge/Google%20Gemini-Multimodal-8E75C2?style=for-the-badge&logo=googlegemini&logoColor=white" alt="Gemini" />
  <img src="https://img.shields.io/badge/Railway-24%2F7-0B0D0E?style=for-the-badge&logo=railway&logoColor=white" alt="Railway" />
  <img src="https://img.shields.io/badge/Docker-Ready-2496ED?style=for-the-badge&logo=docker&logoColor=white" alt="Docker" />
  <img src="https://img.shields.io/badge/License-MIT-44CC11?style=for-the-badge" alt="License" />
</p>

<p align="center">
  <a href="README.md"><b>🌐 English Version</b></a> •
  <a href="docs/HUMANIZATION.md"><b>🌙 مرجع کامل ضدشناسایی</b></a>
</p>

---

<p align="center">
  <b>روح‌گرام</b> یک یوزربات حرفه‌ای، کاملاً نامحسوس (Stealth) و خودکار تلگرام است که حساب کاربری شما را به <b>Google Gemini AI</b> وصل می‌کند.<br/>
  فارسی محاوره‌ای حرف می‌زند، پرسونای قابل تغییر دارد، رفتار خواندن و تایپ کردن انسان را موبه‌مو شبیه‌سازی می‌کند، جواب‌های بلند را تکه‌تکه می‌فرستد، <b>شب‌ها می‌خوابد</b>، <b>گاهی غلط تایپی می‌کند و تصحیحش می‌کند</b>، حافظهٔ بلندمدت دارد، عکس و ویس و ویدئو و PDF را می‌فهمد، و با کدهای مخفی ۳ رقمی خودحذف‌شونده کنترل می‌شود.
</p>

---

</div>

## ✨ قابلیت‌های برجسته

<div dir="rtl">

- 🕶️ **کدهای مخفی خودحذف‌شونده** — کنترل کامل ربات از هر چتی با کدهای ۳ رقمی که بلافاصله پس از ارسال پاک می‌شوند.
- 🤖 **دو حالت اصلی هوش مصنوعی** — حالت رفیق (لحن خودمانی شما) و حالت دستیار (منشی مودب ۲۴ ساعته)، حالا با **دامنهٔ چت‌به‌چت یا سراسری**.
- 🎭 **موتور پرسونای نامحدود** — فایل متنی دلخواه در `personas/*.txt` بساز و بدون ریستارت لحن را عوض کن.
- 🕵️ **تعامل خودکار در گروه‌ها** — بررسی منظم گفتگوها و ورود طبیعی به بحث‌های جالب در فواصل تصادفی.
- ⚡ **شبیه‌ساز رفتار انسانی** — تأخیر خواندن متناسب، زمان تایپ متناسب با طول جواب تا ۴۵ ثانیه، مکس فکر کردن و تکه‌تکه فرستادن جواب‌های بلند.
- 🌙 **ساعات خواب و ضدشناسایی** — شب‌ها ساکت می‌شود (`121`)، گاهی **غلط تایپی می‌فرستد و ثانیه‌ها بعد ویرایشش می‌کند**، گاهی فقط **ریاکشن می‌گذارد جای جواب دادن**، و هرگز ده نفر را در یک ثانیه جواب نمی‌دهد (`122`).
- 👁️ **درک چندرسانه‌ای** — عکس، ویس، ویدیونوت و ویدیو (تا ~۱۸ مگابایت) و فایل‌های PDF و متنی.
- 🧠 **حافظهٔ دو لایه** — ۳۰ پیام آخر با برش هوشمند جملات + خلاصه‌ساز پس‌زمینهٔ فکت‌های کلیدی.
- 🛡️ **استخر کلیدهای جمینای** — چرخش Round-Robin، سقف مصرف روزانه، کنترل نرخ درخواست و Circuit Breaker.
- 🧹 **پاکسازی روح** — حذف دسته‌ای تمام پیام‌های شما با مدیریت خودکار FloodWait.
- 🔌 **معماری افزونه‌ای** — مادول‌های رفتاری خودشان را نصب می‌کنند؛ `main.py` دست‌نخورده می‌ماند و خرابی یک افزونه هرگز ربات را نمی‌خواباند.

</div>

---

## 🎮 راهنمای کدهای رمزی و مخفی

> [!IMPORTANT]
> **امنیت اختصاصی مالک**: تمامی کدهای رمزی تنها از طریق پیام‌های ارسالی خود شما معتبرند. به محض دریافت، **دستور فوراً حذف می‌شود** و تأیید آن موقتاً در «پیام‌های ذخیره‌شده» شما نمایش داده می‌شود (قابل تنطیم با `STEALTH_CONFIRM`).

<div dir="rtl">

| کد مخفی | محدوده | عملکرد |
| :--- | :---: | :--- |
| `777` | چت جاری | **روشن کردن رفیق** با پرسونای پیش‌فرض. |
| `777 <persona>` | چت جاری | **رفیق با لحن خاص** (مانند `777 sarcastic`). |
| `000` / `000 all` | چت / سراسری | **خاموش کردن رفیق**. |
| `777 engage [دقیقه]` | گروه | **تعامل خودکار** (پیش‌فرض هر ۲۰ دقیقه). |
| `777 engage auto` | گروه | 🧠 **تعامل هوشمند**: سرعت گروه را می‌سنجد و فاصله را برای حضور حدود ۵٪ تنطیم می‌کند. |
| `777 engage off [all]` | گروه / سراسری | **خاموش کردن تعامل خودکار**. |
| `666` | فقط همین چت | **روشن کردن دستیار فقط در چت جاری**. |
| `666 all` | تمام پیوی‌ها | **دستیار برای همهٔ پیوی‌ها** (فعلی و آینده). معادل‌ها: `666 کل`، `666 همه`. |
| `444` | چت جاری | **توقف دستیار فقط در این چت**. |
| `444 all` / `444 کل` | تمام پیوی‌ها | **خاموش کردن کامل دستیار**. |
| **`121`** | **سراسری** | 🆕 🌙 **ساعات خواب** — وضعیت، بازه، خواب/بیداری دستی، معافیت چت. |
| **`122`** | **سراسری** | 🆕 🧬 **رفتار انسانی** — غلط تایپی، ریاکشن به‌جای جواب، محدودکنندهٔ انفجار. |
| `!مسدود` (روی پیام) | سراسری | **لیست سیاه**: دستیار دیگر هرگز به این کاربر جواب نمی‌دهد (`!مسدود 123456` هم قبول است). |
| `!آزاد` (روی پیام) | سراسری | **حذف کاربر از لیست سیاه**. |
| `!استیکر <توضیح>` | سراسری | 🎭 **آموزش استیکر**: روی استیکر ریپلای کن و معنی‌اش را بگو. |
| `!استیکر لیست` / `!استیکر حذف` | سراسری | 🎭 مدیریت استیکرهای آموزش‌دیده. |
| `222 [ساعت]` | گروه/پیوی | **خلاصه گفتگو**: اتفاقات ۱۲ ساعت گذشته (تا ۷۲ ساعت) + پیام‌های منتظر جواب. |
| `112 <سوال>` | هرجا | **جستجوی وب** با گوگل (قیمت ارز، آب‌وهوا، اخبار). |
| `111 [دستور]` | ریپلای | **پاسخ هوشمند** به پیام ریپلای‌شده. |
| `555 <یادآور>` | چت جاری | **یادآور هوشمند**: `555 ساعت 21 به علی بگو تماس بگیره`. |
| `333` | چت جاری | **ریست حافظه** کوتاه‌مدت و بلندمدت. |
| `999 [تعداد]` | چت جاری | **پاکسازی روح**: حذف همه (یا N پیام آخر) پیام‌های شما. |
| `555` | چت جاری | **وضعیت زنده** با حذف خودکار. |
| `888` | چت جاری | **راهنما** و لیست کدهای مخفی. |

### 🔔 بازخورد مخفی و گزارش خطا

- تأیید همهٔ دستورها به «پیام‌های ذخیره‌شده» می‌رود و خودبه‌خود پاک می‌شود — هیچ ردی در چت مقصد نمی‌ماند.
- خطاهای موتور یا ارسال هم به همانجا گزارش می‌شوند (`NOTIFY_ERRORS=0` برای غیرفعال کردن).

### 👁️ درک چندرسانه‌ای

وقتی حالت «رفیق» (`777`) یا «دستیار» (`666`) فعال باشد، ربات محتوای 📷 عکس، 🎙️ ویس، 🎬 ویدیونوت و ویدیو (تا ~۱۸ مگابایت) و 📄 فایل‌های PDF و متنی را می‌فهمد و طبیعی جواب می‌دهد.

*(یادآورها در `reminders_state.json` ذخیره می‌شوند و با ری‌استارت از بین نمی‌روند.)*

</div>

---

## 🎯 دامنهٔ دستیار: `666` یا `666 all`

<div dir="rtl">

قبلاً دستیار یا در همه‌جا روشن بود یا هیچ‌جا. حالا **دو لایه** دارد تا بتوانی منشی را فقط برای یک نفر خاص بگذاری بدون اینکه همهٔ پیوی‌هایت درگیر شوند.

| دستور | نتیجه |
|---|---|
| `666` | دستیار **فقط در همان چتی که فرستادی** روشن می‌شود. |
| `666 all` | دستیار برای **تمام پیوی‌ها** روشن می‌شود، شامل کسانی که بعداً پیام می‌دهند. |
| `666 <چیز دیگر>` | با هشدار رد می‌شود — یک غلط تایپی هرگز تو را بی‌سروصدا به حالت سراسری نمی‌برد. |
| `444` | دستیار در این چت ساکت می‌شود (و از لیست چت‌های فعال حذف می‌شود). |
| `444 all` | دستیار کاملاً خاموش می‌شود: حالت سراسری، لیست چت‌ها و سکوت‌ها پاک می‌شوند. |

داشبورد `555` دقیقاً می‌گوید کدام لایه فعال است و چند چت تک‌به‌تک روشن شده‌اند.

> [!WARNING]
> **مهاجرت یک‌باره.** فایل‌های وضعیتی که قبل از این تغییر نوشته شده‌اند در اولین اجرا ارتقا می‌یابند و دستیار **خاموش** می‌شود تا بی‌سروصدا به همهٔ پیوی‌ها جواب ندهد. بعد از آپدیت، در چت‌هایی که می‌خواهی `666` بزن یا یک‌بار `666 all` بفرست. لیست سیاه و چت‌های ساکت‌شده دست‌نخورده می‌مانند.

</div>

---

## ⚡ شبیه‌ساز رفتار انسانی

<div dir="rtl">

بزرگ‌ترین نشانهٔ روبات بودن یک یوزربات این نیست که *چه* می‌نویسد؛ این است که *چقدر سریع* می‌نویسد. یک پاراگراف ۴۰۰ کاراکتری که بعد از ۳ ثانیه بیاید، برای انگشت انسان غیرممکن است. پس کل خط لولهٔ ارسال انسانی‌سازی شده است:

۱. **تأخیر خواندن متناسب** پیش از شروع تایپ، تا واقعاً مثل خواندن پیام به نظر برسد.
۲. **زمان تایپ متناسب با طول متن** روی منحنی غیرخطی، با سقف **۴۵ ثانیه** جای سقف خشک ۷ ثانیه‌ای قدیم.
۳. **مکس‌های تصادفی فکر کردن** در میانهٔ تایپ، تا علامت typing درست مانند انسان روشن و خاموش شود.
۴. **تکه‌تکه فرستادن خودکار**: جواب‌های بلندتر از آستانه از مرز جملات بریده و به صورت **چند پیام جداگانه** فرستاده می‌شوند. کد و استیکر هرگز تکه نمی‌شوند.
۵. **پالایشگر خروجی ضد ربات**: حذف ایموجی، اعراب عربی، تگ‌های HTML و لیست‌های مارک‌داون پیش از ارسال.

**زمان‌بندی تقریبی:**

| طول جواب | زمان تایپ |
|---|---|
| ~۵ کاراکتر | ~۱.۲ ثانیه |
| ~۵۰ کاراکتر | ~۱۵ ثانیه |
| ~۱۵۰ کاراکتر | ~۵۰ ثانیه (تکه‌تکه می‌شود) |
| بیش از ۴۰۰ کاراکتر | سقف ۴۵ ثانیه برای هر حباب |

تمام این اعداد با متغیرهای `HUMAN_*` قابل تنطیم‌اند.

</div>

---

## 🌙 ساعات خواب و رفتار ضدشناسایی

<div dir="rtl">

زمان‌بندی فقط نیمی از ماجراست. یک یوزربات که **۲۴ ساعته بیدار است**، **هرگز اشتباه نمی‌کند** و **ده نفر را در یک ثانیه جواب می‌دهد**، هنوز واضحاً ماشین است. دو افزونهٔ خودنصب دقیقاً این را حل می‌کنند و هر دو **زنده** قابل تنظیم‌اند — بدون نیاز به Redeploy.

> 📖 **مرجع کامل:** [`docs/HUMANIZATION.md`](docs/HUMANIZATION.md) — همهٔ دستورها، همهٔ متغیرها، خط لولهٔ ارسال و جدول عیب‌یابی.

### 🌙 ساعات خواب (`121`)

ربات در بازه‌ای که خودت تعیین می‌کنی ساکت می‌ماند.

| دستور | کار |
|---|---|
| `121` | وضعیت: بازه، ساعت محلی ربات، چقدر تا بیداری |
| `121 01:00 09:00` | تنظیم بازهٔ خواب و روشن کردن |
| `121 on` / `121 off` | روشن / خاموش |
| `121 tz +03:30` | تنظیم منطقهٔ زمانی |
| `121 sleep 90` | همین الان ۹۰ دقیقه بخواب (مانند وقتی جلسه داری) |
| `121 wake 60` | ۶۰ دقیقه بیدار بمان، حتی در بازهٔ خواب |
| `121 now` | لغو خواب/بیداری دستی |
| `121 allow` | معاف کردن همین چت — شب‌ها هم جواب می‌گیرد |

فرمت ساعت انعطاف‌پذیر است: `23:30` یا `2330` یا `23`، ارقام فارسی هم قبول می‌شود، و بازه‌هایی که از نیمه‌شب عبور می‌کنند درست کار می‌کنند.

> [!IMPORTANT]
> ساعات خواب فقط جلوی **جواب‌های خودکار** (رفیق، تعامل خودکار، دستیار) را می‌گیرد. **دستورهای خودت همیشه کار می‌کنند** — `111`، `112`، `222`، `555`، `999` ساعت ۴ صبح دقیقاً مثل وسط روز عمل می‌کنند.

### 🧬 رفتار انسانی (`122`)

۱. **غلط تایپی و تصحیح** — با احتمال کم، ربات جای دو حرف را عوض می‌کند یا حرفی را می‌اندازد، و چند ثانیه بعد یا **پیام را ویرایش می‌کند** (تلگرام برچسب `edited` را نشان می‌دهد — نشانهٔ بسیار انسانی) یا `*کلمهٔ درست` می‌فرستد. لینک، منشن، عدد و جواب‌های چندخطی هرگز دستکاری نمی‌شوند.
۲. **ریاکشن به‌جای جواب** — وقتی جواب یک جملهٔ بی‌ارزش است (`خخخ`، `باشه`، `مرسی`) و ریپلای به پیامی خاص است، متن حذف می‌شود و ربات فقط **ایموجی متناسب می‌گذارد**. سوال‌ها همیشه متنی جواب داده می‌شوند؛ اگر ریاکشن ناموفق باشد، متن عادی فرستاده می‌شود.
۳. **محدودکنندهٔ انفجار** — سقف سراسری و فاصلهٔ حداقل بین جواب‌های خودکار، تا ده پیام ورودی ده جواب فوری تولید نکند.

| دستور | کار |
|---|---|
| `122` | وضعیت هر سه رفتار |
| `122 on` / `122 off` | روشن / خاموش کردن کل پکیج |
| `122 typo 8` | احتمال غلط تایپی (درصد) |
| `122 style edit\|star\|mixed` | سبک تصحیح غلط |
| `122 react 20` | احتمال ریاکشن به‌جای جواب (درصد) |
| `122 burst 3 60` | حداکثر ۳ جواب خودکار در ۶۰ ثانیه |
| `122 spacing 6` | حداقل ۶ ثانیه فاصله بین دو جواب |
| `122 reset` | بازگشت به پیش‌فرض‌ها |

### 🔌 چطور وصل می‌شوند

هر دو مادول در انتهای `pal_manager.py` ایمپورت می‌شوند و خودشان را نصب می‌کنند: `quiet_hours` توابع تصمیم‌گیری جواب خودکار را و `human_behavior` تابع `send_message` را (روی پچ `typing_helper`) می‌پوشانند، و کد مخفی خود را روی کلاینت ثبت می‌کنند. **`main.py` اصلاً تغییر نکرده** و اگر افزونه‌ای لود نشود، ربات فقط یک هشدار لاگ می‌کند و مثل قبل کار می‌کند.

لاگ اولیه باید این دو خط را داشته باشد:

```
🌙 Quiet Hours ready — ❌ غیرفعال | کد 121
🧬 Human Behavior ready — ✅ غلط 8٪ | واکنش 22٪ | سقف 3/60ث
```

</div>

---

## 🎭 حالت‌های هوش مصنوعی و پرسونای پویا

<div dir="rtl">

### ۱. حالت رفیق (Alter-Ego)
با کد `777` ربات درست مثل خود شما رفتار می‌کند؛ لحن عامیانه، پاسخ‌های کوتاه بدون ایموجی و ارجاع دقیق به سوابق گفتگو. در گروه‌ها تنها وقتی جواب می‌دهد که روی شما ریپلای بزنند یا منشنتان کنند.

### ۲. حالت دستیار شخصی
با `666` برای یک چت و با `666 all` برای همهٔ پیوی‌ها فعال می‌شود. نقش منشی موقر شما را بازی می‌کند، به مراجعین پاسخ محترمانه می‌دهد و پیام می‌گیرد.

### ۳. تعامل خودکار در گروه‌ها (Auto-Engage)
با `777 engage <دقیقه>` ربات سوابق گروه را هر چند دقیقه یک‌بار بررسی می‌کند و اگر بحث جالبی در جریان باشد، هوشمندانه وارد می‌شود. با `777 engage auto` فاصله را خودش بر اساس سرعت واقعی گروه انتخاب می‌کند.

### ۴. تعویض زندهٔ پرسوناها
کافیست هر فایل `.txt` را درون پوشهٔ `personas/` قرار دهید:

```
personas/
├── normal.txt          <- لحن عامیانه و پیش‌فرض
├── assistant.txt       <- لحن منشی محترمانه
├── sarcastic.txt       <- لحن طعنه‌زن و تیکه‌دار
├── poetic.txt          <- لحن شاعرانه و دراماتیک
├── academic.txt        <- لحن رسمی و علمی
├── angry.txt           <- لحن عصبی و رک
├── drunk.txt           <- لحن صمیمی و بی‌فیلتر
└── lust.txt            <- لحن محبت‌آمیز و دلنشین
```

> [!TIP]
> برای لحن جدید کافیست فایلی مانند `personas/gamer.txt` بسازید و با `777 gamer` فعالش کنید — بدون ریستارت!

</div>

---

## 🧬 حافظهٔ دو لایه

<div dir="rtl">

- **حافظهٔ کوتاه‌مدت**: واکشی ۳۰ پیام اخیر با ساختار فرستنده، زمان و رشتهٔ ریپلای‌ها.
- **خلاصه‌ساز بلندمدت پس‌زمینه**: هر ۳۰ پیام، مهم‌ترین فکت‌ها فشرده و در `memory_state.json` ذخیره می‌شوند.
- **ریست آنی حافظه (`333`)**: کلیهٔ سوابق کوتاه‌مدت و خلاصهٔ بلندمدت آن چت فوراً پاک می‌شود.

</div>

---

## 🛡️ موتور مدیریت کلیدهای جمینای

<div dir="rtl">

- **چرخش خودکار کلیدها (Round-Robin)**: تعداد نامحدود کلید در `apis.txt` یا `.env`.
- **سقف دلخواه هر مدل**: با `GEMINI_MODELS="name:rpm:rpd,..."` و پیش‌فرض‌های `DEFAULT_MODEL_RPM` / `DEFAULT_MODEL_RPD`.
- **شمارنده سهمیه روزانه**: ثبت مصرف هر کلید در `api_usage.json` با سقف ایمن زیر حد گوگل.
- **Circuit Breaker**: کلیدی که ۳ بار پیاپی خطا بدهد، ۳ ساعت قرنطینه می‌شود.
- **تایم‌اوت سخت**: `GEMINI_TIMEOUT` و بازهٔ طولانی‌تر `SEARCH_TIMEOUT` برای جستجوی وب.

</div>

---

## 🚀 راه‌اندازی و نصب سریع

<div dir="rtl">

### روش اول: Railway (پیشنهادی برای اجرای ۲۴ ساعته)

۱. این ریپو را فورک/کلون کنید و یک سرویس Railway از روی آن بسازید.
۲. یک **Volume** بسازید و در مسیر `/app/data` وصل کنید — بدون آن، حافظه، یادآورها، وضعیت مودها و تنظیمات `121`/`122` با هر ری‌استارت پاک می‌شوند.
۳. `python3 gen_session_string.py` را **روی سیستم خودتان** اجرا کنید و خروجی را در `SESSION_STRING` بگذارید.
۴. متغیرهای `API_ID`، `API_HASH`، `GEMINI_API_KEYS`، `OWNER_ID` را تنظیم کنید.
۵. دیپلوی کنید. لاگ اولیه وضعیت رفیق/دستیار و لیست کدهای فعال را چاپ می‌کند.

> [!TIP]
> اگر Railway خطای **«GitHub Repo not found»** نشان داد، یعنی اپ Railway دسترسی‌اش به ریپو را از دست داده. به <https://github.com/settings/installations> بروید ← Railway ← دسترسی به این ریپو را بدهید، سپس Source Repo را قطع و وصل کنید و Redeploy بزنید.

### روش دوم: ویزارد نصب آسان (ویندوز)

```powershell
# روی setup.bat دوبار کلیک کنید یا:
python setup.py
```

### روش سوم: نصب دستی و محلی (CLI)

```bash
git clone https://github.com/parhambepe/ghostali.git
cd ghostali
python -m venv venv
source venv/bin/activate   # در ویندوز: venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env
python login.py
python main.py
```

### روش چهارم: اجرا با داکر

```bash
docker compose up -d --build
docker compose logs -f
```

### روش پنجم: استقرار ۲۴ ساعته روی سرور لینوکس (VPS)

اسکریپت‌های `deploy.bat` و `deploy.sh` پروژه را بسته‌بندی کرده، روی سرور آپلود نموده و به عنوان سرویس `systemd` اجرا می‌کنند. کافیست در `.env` مقادیر `VPS_IP`، `SSH_USER`، `SSH_PORT` را وارد کنید؛ پروژه روی `/opt/ghostgram` مستقر می‌شود.

</div>

---

## ⚙️ تنطیمات محیطی (.env)

<div dir="rtl">

```ini
# 📱 تلگرام (https://my.telegram.org/apps)
API_ID=12345678
API_HASH=abcdef0123456789abcdef0123456789
PHONE_NUMBER=+989****6789
OWNER_ID=123456789
PERSONA_NAME=شایان

# 🧠 جمینای (https://aistudio.google.com)
GEMINI_API_KEYS=AIzaSyA...Key1,AIzaSyB...Key2
MODEL_NAME=gemini-3.5-flash-lite
GEMINI_MODELS=
DEFAULT_MODEL_RPM=15
DEFAULT_MODEL_RPD=200
SESSION_NAME=ghostgram_session

# ☁️ دیپلوی ابری Railway
# یک بار روی سیستم خودت: python3 gen_session_string.py
SESSION_STRING=
DATA_DIR=/app/data

# 🔔 بازخورد مخفی و گزارش خطا
STEALTH_CONFIRM=1
CONFIRM_AUTO_DELETE_SECONDS=10
NOTIFY_ERRORS=1

# 🧠 حافظه و رفتار
SHORT_TERM_MEMORY_LIMIT=30
LONG_TERM_SUMMARY_INTERVAL=30
MAX_LONG_TERM_SUMMARY_CHARS=600
MAX_MESSAGE_SEGMENT_CHARS=200

# ⏱ تایم‌اوت موتور
GEMINI_TIMEOUT=15
SEARCH_TIMEOUT=45

# ⌨️ موتور ارسال انسانی‌شده (typing_helper.py)
HUMAN_MIN_TYPING_DELAY=1.2      # کف تأخیر برای جواب‌های خیلی کوتاه (ثانیه)
HUMAN_MAX_TYPING_DELAY=45.0     # سقف تأخیر برای جواب‌های بلند (ثانیه)
HUMAN_TYPING_CPS_SCALE=1.0      # بالاتر از 