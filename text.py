class Text:
    HELP = """
🕶️ **کدهای مخفی GhostGram (مخصوص خودت)** 🕶️
همهٔ کدها مخفی‌اند؛ به‌محض ارسال پاک می‌شوند و تأییدشان به «پیام‌های ذخیره‌شده» می‌رود.

🔮 **رفیق (PAL):**
• `777` روشن کردن رفیق (پاسخ خودکار با لحن عادی)
• `777 <حالت>` روشن با حالت خاص: lust، lust afghani، tajik، angry، philosophical، sarcastic، depressed، drunk، academic، tehrani، poetic
• `000` خاموش کردن در همین چت | `000 all` در کل چت‌ها

🕵️ **تعامل خودکار (AUTO ENGAGE):**
• `777 engage auto` تعامل هوشمند (سرعت چت را می‌سنجد و فاصله را خودش تنظیم می‌کند)
• `777 engage` یا `777 engage 20` تعامل خودکار (هر ۲۰ دقیقه — یا عدد دلخواه — ورود هوشمندانه به گروه)
• `777 engage off [all]` خاموش کردن تعامل (این گروه / همهٔ گروه‌ها)

🤖 **دستیار شخصی (ASSISTANT):**
• `666` روشن کردن دستیار فقط همین چت | `666 all` یا `666 کل` سراسری
• `444` توقف دستیار فقط همین چت | `444 all` یا `444 کل` همهٔ چت‌ها
• `!مسدود` (روی پیام) افزودن به لیست سیاه | `!آزاد` حذف از لیست سیاه

🎭 **استیکرها:**
• `!استیکر <توضیح>` (روی ریپلای استیکر) آموزش معنای استیکر — بعدش خودش می‌فهمد و استفاده می‌کند
• `!استیکر لیست` نمایش آموخته‌شده‌ها | `!استیکر حذف` (روی ریپلای) حذف از پول ارسال

🛠 **ابزارها:**
• `333` ریست حافظه این چت
• `999` یا `999 20` پاکسازی پیام‌های ارسالی شما (کل / ۲۰ پیام آخر)
• `111` (روی ریپلای) پاسخ هوشمند | `111 <دستور>` پاسخ طبق دستور
• `222` یا `222 24` خلاصه گفتگوی گروه (۱۲ ساعت اخیر / عدد دلخواه)
• `112 <سؤال>` جستجوی وب (روی پیام هم می‌شود ریپلای کرد)
• `555 <یادآور>` یادآور هوشمند (مثلاً `555 ساعت 21 به علی بگو تماس بگیره`) | `555` وضعیت رفیق و دستیار
• `888` نمایش این راهنما

🌙 **ساعات خواب (121):**
• `121` وضعیت | `121 01:00 09:00` تنظیم بازه و روشن کردن | `121 on` / `121 off`
• `121 tz +03:30` منطقهٔ زمانی | `121 sleep 90` خواب فوری | `121 wake 60` بیدار ماندن
• `121 now` لغو دستی | `121 allow` معاف کردن همین چت (شب‌ها هم جواب می‌گیرد)

🧬 **رفتار انسانی (122):**
• `122` وضعیت | `122 on` / `122 off` کل پکیج | `122 reset` بازگشت به پیش‌فرض
• `122 typo 8` غلط تایپی (٪) | `122 style edit|star|mixed` سبک تصحیح
• `122 react 20` ریاکشن به‌جای جواب (٪) | `122 burst 3 60` حداکثر ۳ جواب در ۶۰ ثانیه | `122 spacing 6` حداقل فاصله (ثانیه)

📊 `130` گزارش مصرف امروز Gemini + آپ‌تایم ربات و حجم حافظه

💾 **بکاپ حافظه (133):**
• `133` وضعیت | `133 now` بکاپ فوری | `133 time 04:00` ساعت روزانه
• `133 send on/off` ارسال خودکار | `133 keep 7` تعداد نسخه‌ها | `133 list` لیست بکاپ‌ها
• `133 restore` ارسال آخرین بکاپ برای دانلود | `133 on` / `133 off`

👁️ چندرسانه‌ای: عکس، ویس، ویدیو نوت و PDF متنی را می‌فهمد؛ استیکرهای آموزش‌دیده را هم می‌شناسد و استفاده می‌کند.
*(تأیید همه دستورهای مخفی + گزارش خطاها به «پیام‌های ذخیره‌شده» ارسال می‌شود)*
"""
    PAL_ACTIVATED = "🤖 **حالت رفیق برای این چت فعال شد.**\nاز این پس پیام‌های دریافتی به صورت خودکار، با شبیه‌سازی تایپ و لحن طبیعی پاسخ داده می‌شوند."
    PAL_DEACTIVATED = "💤 **حالت رفیق برای این چت غیرفعال شد.**"
    PAL_STATUS_ACTIVE = "🟢 **وضعیت رفیق:** در این چت **فعال** است."
    PAL_STATUS_INACTIVE = "⚪ **وضعیت رفیق:** در این چت **غیرفعال** است."

    ASSISTANT_STATUS_ACTIVE = "🟢 **وضعیت دستیار شخصی:** در این چت **فعال** است."
    ASSISTANT_STATUS_INACTIVE = "⚪ **وضعیت دستیار شخصی:** **غیرفعال** است."


    PURGE_START = "🧹 **در حال پاکسازی پیام‌های شما...**"
    PURGE_DONE = "✅ تعداد `{count}` پیام ارسالی شما با موفقیت از این گفتگو حذف شد."
    PURGE_NONE = "ℹ️ پیامی از شما برای حذف در این گفتگو پیدا نشد."

    PROCESSING = "در حال تفکر..."
    ERROR = "خطایی رخ داد. لطفا دوباره تلاش کنید."
    ERROR_PREFIX = "Error: "
    CHAIN_TEMPLATE = "[{time}] {sender}: {message}"
    NO_TEXT = "[رسانه / بدون متن]"
    UNKNOWN_SENDER = "مخاطب"
    ME_LABEL = "من"

    # --- Blacklist feedback (sent to Saved Messages, auto-deleted) ---
    BLACKLIST_ADDED = "🚫 کاربر با ID `{user_id}` به لیست سیاه اضافه شد (دستیار دیگر پاسخ نمی‌دهد)."
    BLACKLIST_REMOVED = "✅ کاربر با ID `{user_id}` از لیست سیاه حذف شد."
